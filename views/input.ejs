<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/image/logo.jpeg" type="image/x-icon">

    <title>Error Found</title>
    <style>
        body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #542eec;
            font-family: Arial, sans-serif;
        }

        .error-container {
            text-align: center;
            background-color: #ffffff;
            border: 1px solid #f5c6cb;
            padding: 20px;
            border-radius: 10px;
            /* height: 23vh; */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .error-icon {
            width: 100px;
            height: 100px;
        }

        .error-message {
            margin-top: 20px;
            font-size: 24px;
            color: #721c24;
        }

        .back-button {
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #dc3545;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
        }

        .back-button:hover {
            background-color: #c82333;
        }
    </style>
</head>

<body>
    <div class="error-container">
        <img src="/image/Error.png" alt="Error Found" class="error-icon">
        <div class="error-message">You have enter wrong input.</div>
        <a href="/login" class="back-button">Go Back Login</a>
    </div>
</body>

</html>